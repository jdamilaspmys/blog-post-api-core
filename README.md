# blog-post-api-core

This is a simple Express.js project generated by the Express generator. It demonstrates a basic
application structure including routes, views and static assets.

## Usage

Install dependencies and start the server:

```bash
npm install
npm start
```

The application will be available at `http://localhost:3000/` by default.
